'use babel';

import RunTestInTerminalView from '../lib/run-test-in-terminal-view';

describe('RunTestInTerminalView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
